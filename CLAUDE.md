# Витрина профиля — свод для агентского окна

> Этот файл читается при старте **каждого** окна и попадает в контекст целиком.
> Поэтому здесь компактные триггеры и ссылки на канон, а не пересказ.
> → [`029`](https://github.com/ArtVsMark/claude-code-playbook/blob/main/rules/ru/029-triggers-and-canon.md),
> [`022`](https://github.com/ArtVsMark/claude-code-playbook/blob/main/rules/ru/022-one-canonical-document.md)
>
> Он появился, потому что окна открывались с контекстом проекта, но **без
> правил**, и находили их по одному — обычно уже нарушив.

## 🚦 Критические запреты (читать первым)

```
❌ НЕ сливать PR из окна — прокси подменяет учётные данные на записи, автором
   станет бот. Слияние живёт в Actions
❌ НЕ вписывать числа в текст витрины руками — только между маркерами m:ключ
❌ НЕ возвращать <details> на витрину — там, где страницу читают текстом, он
   схлопывается в заголовок без содержимого
❌ НЕ открывать PR без метки содержания
❌ НЕ везти в одном PR несколько тем
❌ НЕ переписывать историю main
❌ НЕ утверждать на витрине то, чего читатель не может проверить
❌ НЕ брать на витрину гифки и картинки самого проекта — решение владельца
```

Четыре из этих запретов держат гейты, остальные — нет. Непроверяемые названы
здесь именно поэтому, а не потому что «и так понятно».
→ [`057`](https://github.com/ArtVsMark/claude-code-playbook/blob/main/rules/ru/057-unmechanizable-rules-are-named-explicitly.md)

## 🪟 Окно

**Срок жизни — три–пять дней**, дальше эстафета ссылками, а не пересказом.
Заготовка стартового сообщения — [`session-opening.md`](https://github.com/ArtVsMark/claude-code-playbook/blob/main/templates/session-opening.md).
→ [`006`](https://github.com/ArtVsMark/claude-code-playbook/blob/main/rules/ru/006-window-lifetime.md)

**Сменились правила работы — окна перезапускаются.** Этот файл читается один раз,
при старте: правка не действует на уже запущенное окно.
→ [`047`](https://github.com/ArtVsMark/claude-code-playbook/blob/main/rules/ru/047-rule-change-restarts-the-windows.md)

Витрину ведут только облачные окна: локального окружения у неё нет, потому что
нет ничего, что нужно было бы запускать на живой машине. Имя окна начинается с
окружения — `[WEB]`.
→ [`038`](https://github.com/ArtVsMark/claude-code-playbook/blob/main/rules/ru/038-window-name-declares-its-environment.md)

## 🛡 Гейты

**Перед пушем:** `python scripts/build_metrics.py <клон грейдера> --check` — то
же, что проверит конвейер, только раньше. Ничего не пишет.

**На PR:** `pr-check.yml` — сборка вхолостую и классификация
(`scripts/check_labels.py`).

Исходов у проверки три, а не два: собралось · не собралось с названной
причиной · **источник не ответил**. Третий не молчит.
→ [`039`](https://github.com/ArtVsMark/claude-code-playbook/blob/main/rules/ru/039-three-outcomes-not-two.md)

## 🔀 Слияние

Очереди мержа здесь **нет** — PR идут по одному, и заводить очередь дороже
пользы. Порядок: открыть PR готовым → дождаться зелёного → конвейер сольёт сам.

- **автомерж по умолчанию**: отключается меткой `hold` или черновиком, а не
  включается флажком;
- **сообщение пересобирается** из тел коммитов ветки вместе с трейлерами:
  кнопка «Merge pull request» теряет и то и другое
  → [`123`](https://github.com/ArtVsMark/claude-code-playbook/blob/main/rules/ru/123-attribution-is-verified-on-the-final-history.md);
- **автор коммита слияния — человек**: нужен секрет `MERGE_TOKEN`, штатный
  токен сделал бы автором бота.

## 🏷️ Метки

Обязательны и делятся на два класса, которые нельзя смешивать: **содержание**
ставит автор (`bug`, `enhancement`, `documentation`, `github_actions`,
`dependencies`), **конвейер** — `hold`, и требовать её от автора нельзя.
→ [`064`](https://github.com/ArtVsMark/claude-code-playbook/blob/main/rules/ru/064-labels-are-machine-input-not-decoration.md)

Связь с задачей обязательной не сделана: у витрины бэклог из одной-двух задач,
и большая часть работы задачами не заводится. Это осознанный вырез, а не
пропуск.

## 🇷🇺 Язык артефактов

**Витрина — по-английски**, с кратким русским пересказом в конце страницы: её
читает англоязычный посетитель профиля.

**Всё служебное — по-русски**: коммиты, описания PR, `.rules/`, `HISTORY.md`,
комментарии в коде. Их читают владелец и окно.

## 🤖 Параллельные исполнители

Не используются: над витриной работает одно окно. Волны, задания исполнителям и
сборщики результатов здесь предмета не имеют — если появятся, правила берутся из
каталога, а не изобретаются.

## 📝 Журнал изменений

[`HISTORY.md`](HISTORY.md) — рассказом: что ломалось, чем закрыли, почему
решения такие. Пополняется тем же заходом, что и правка, а не перед выпуском.

Фрагментов, как в грейдере, здесь нет намеренно: они окупаются там, где
параллельные ветки правят общий файл, а тут один автор и одна ветка.
→ [`030`](https://github.com/ArtVsMark/claude-code-playbook/blob/main/rules/ru/030-changelog-from-fragments.md)

## 📚 Источники истины (не дублировать)

| Тема | Канонический документ |
|---|---|
| что показывает витрина | [`README.md`](README.md) |
| список проектов и их порядок | [`projects.json`](projects.json) |
| ответ по каждому правилу каталога | [`.rules/bindings.json`](.rules/bindings.json) |
| атрибуция, конвейер, названные пробелы | [`.rules/README.md`](.rules/README.md) |
| роли и пласты | [`.rules/roles.md`](.rules/roles.md) |
| как страница дошла до нынешнего вида | [`HISTORY.md`](HISTORY.md) |
| почему правило именно такое | каталог, по номеру из `bindings.json` |

## 🎯 Открытая работа

Сверху вниз, **первый непустой источник и есть план**:

1. задачи этого репозитория — единственный источник статусов;
2. `unreviewed` в [`.rules/bindings.json`](.rules/bindings.json) — правила,
   которых ещё не разбирали;
3. названные пробелы в [`.rules/README.md`](.rules/README.md) и
   [`.rules/roles.md`](.rules/roles.md) — то, что признано непокрытым.

Пустой трекер — нормальное состояние, а не признак, что работа кончилась.
→ [`091`](https://github.com/ArtVsMark/claude-code-playbook/blob/main/rules/ru/091-work-sources-are-ordered-first-non-empty-wins.md)

## 📓 Новое правило — в общий каталог

Правило, выстраданное здесь, уезжает в
[каталог](https://github.com/ArtVsMark/claude-code-playbook) **тем же заходом**,
а не «потом». Здесь остаются формулировка и вердикт в `bindings.json`; инцидент
и границы живут там.

Не идёт в каталог то, у чего не нашлось инцидента: это предпочтение, а не
правило. → [`080`](https://github.com/ArtVsMark/claude-code-playbook/blob/main/rules/ru/080-every-new-rule-goes-into-the-catalogue.md)

## 🎭 Роли

Владелец решает и держит секреты, окно делает, критик спрашивает «что мы
пропустили». Третью роль пока играет владелец — это названо, а не
подразумевается. Подробно, вместе с непокрытыми пластами —
[`.rules/roles.md`](.rules/roles.md).

Возражение — часть роли, а не дерзость: окно обязано сказать владельцу «это
нельзя проверить, значит это обещание», а владелец окну — «ты отклонил правило
в свою пользу».

## ✅ Перед PR

```
[ ] python scripts/build_metrics.py <клон грейдера> --check — зелёный
[ ] метка содержания проставлена
[ ] PR открыт готовым, не черновиком
[ ] в PR одна тема
[ ] числа на витрине не вписаны руками
[ ] вердикты в .rules/bindings.json обновлены, если правило стало действовать
[ ] HISTORY.md пополнен, если через полгода спросят «почему так»
[ ] выстраданное правило уехало в каталог
```
