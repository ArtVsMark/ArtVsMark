# Журнал изменений

Что менялось на витрине, коротко и по одной строке на изменение.

<!--
НОВАЯ ЗАПИСЬ — ФАЙЛОМ: `changelog.d/<слаг>.<секция>.md`, одна строка текста без
ведущего дефиса, номер изменения в тексте. Два файла с разными именами не
конфликтуют никогда — а именно этим кончались две ветки одного дня, когда обе
дописывали в общий журнал. Формат и команды — `changelog.d/README.md`.

Соглашение общее с грейдером, каталогом правил и `Claude-Code_Usage-Token`:
одно на четыре проекта дешевле четырёх.

ПОЧЕМУ ЗДЕСЬ НЕТ ВЕРСИЙ. У витрины нет выпусков: она живёт непрерывно, и
привязывать записи к версии, которой не существует, значило бы выдумывать
рубеж. Раздел закрывается датой, когда накопленное перестаёт помещаться в
обозримое, — и это решение владельца, а не расписание.
-->

## [Не выпущено]
