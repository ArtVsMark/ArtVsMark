прогон открытия изменений называется `agent-pr`, как ветки `agent/<слаг>` и как у трёх соседних проектов: витрина оставалась единственной, где он звался `open-pr` — проверено запросом к каждому из четырёх (#212)
