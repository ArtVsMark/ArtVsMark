состав витрины сверяется со списком репозиториев владельца: незаявленный проект называется, а решение «не показывать» живёт данными с причиной — прежде сборка каждый прогон видела все репозитории, считала их число и выбрасывала имена
