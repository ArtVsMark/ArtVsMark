критерии качества профилей перестали держаться памятью: контраст текста считается по WCAG на каждой картинке витрины, `alt` страницы сверяется с `aria-label` внутри неё, ссылки на чужие действия проверяются на SHA, а сторож свежести спрашивает и про прогон змейки
