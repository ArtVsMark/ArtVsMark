ручная кнопка `open-pr` открывает изменение по названной ветке, а не по той, на которой запущен прогон: голова бралась у `$GITHUB_SHA`, и запуск с `main` всегда отвечал «открывать нечего» — признак отвергает гейт
